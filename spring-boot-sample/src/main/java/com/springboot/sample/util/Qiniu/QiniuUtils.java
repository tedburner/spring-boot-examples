package com.springboot.sample.util.Qiniu;

/**
 * @author Lucifer
 * @data 2018/5/7
 * @Description: 七牛云
 */
public class QiniuUtils {

    /**
     * 生成key
     */
    public static String getImageKey(String img) {
//        String postfix = "";
//        String originalFilename = img.getOriginalFilename();
//        if (!originalFilename.isEmpty() && originalFilename.lastIndexOf(".") != -1) {
//            postfix = originalFilename.substring(originalFilename.lastIndexOf(".") + 1);
//        }
//
//        if (postfix.isEmpty()) {
//            postfix = "png";
//        }
//
//        String imgKey = new SimpleDateFormat("yyyyMMddHHmmss").format(new Date()) + MD5.selectRandom(6) + "." + postfix;
        return "";
    }
}
