<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springboot.persist.UserMapper">

    <sql id="Base_Column_List">
        id, name, age
    </sql>

    <insert id="addUser" parameterType="com.example.springboot.model.DO.UserDO"
    useGeneratedKeys="true" keyProperty="id">
        INSERT INTO  cityDO (name,age)
        VALUE (#{name},#{age})
    </insert>

    <select id="selectUser" resultType="com.example.springboot.model.DO.UserDO">
        select
        <include refid="Base_Column_List" />
        from user

    </select>

</mapper>